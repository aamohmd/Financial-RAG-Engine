services:
  fastapi:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    command: ["fastapi", "dev", "rag/api.py", "--port", "8080", "--host", "0.0.0.0"]
